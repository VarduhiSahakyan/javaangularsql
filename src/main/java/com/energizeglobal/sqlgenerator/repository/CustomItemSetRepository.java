package com.energizeglobal.sqlgenerator.repository;

import com.energizeglobal.sqlgenerator.domain.CustomItemSetEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CustomItemSetRepository extends JpaRepository<CustomItemSetEntity,Long> {
}
